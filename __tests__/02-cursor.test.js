const typer = require('../typer');

describe(`Testing the '.cursor' method`, () => {
  beforeEach(() => document.body.innerHTML = '<div id="test"></div>');
  afterEach(() => document.body.innerHTML = '');

  test('cursor should do something', () => {

  });
});